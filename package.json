{
  "name": "optimize-dns-worker",
  "version": "0.0.1",
  "description": "cloudflare worker and dns optimize",
  "main": "ns.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
